# SCUT WALK TALL — 源代码文档

本文档详细说明项目中每个源代码文件（.h / .cpp）的结构、变量、函数及关联关系。

---

## 目录

1. [入口文件](#1-srcmaincpp)
2. [游戏主控制器](#2-srcgamegameh--srcgamegamecpp)
3. [玩家系统](#3-玩家系统)
4. [敌人系统](#4-敌人系统)
5. [技能系统](#5-技能系统)
6. [道具系统](#6-道具系统)
7. [地图系统](#7-地图系统)

---

## 1. src/main.cpp

### 文件说明
游戏入口文件，负责创建 Game 实例并启动游戏主循环。

### 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| game | Game | 游戏主控制器实例 |

### 函数
| 函数名 | 参数 | 返回值 | 说明 |
|--------|------|--------|------|
| main | void | int | 程序入口，创建 Game 对象并调用 run() |

### 关联文件
- `src/game/Game.h` — 引用 Game 类

---

## 2. src/game/Game.h / src/game/Game.cpp

### 文件说明
游戏主控制器，持有并协调所有子系统（窗口、场景、角色、敌人、技能、地图等），管理游戏状态流转。

### Game.h 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| DesignWidth / DesignHeight | unsigned int | 设计分辨率（1920×1080） |
| m_window | sf::RenderWindow | SFML 渲染窗口 |
| m_clock | sf::Clock | 游戏时钟 |
| m_running | bool | 游戏运行标志 |
| m_isFullscreen | bool | 全屏模式标志 |
| m_state | GameState | 当前场景状态（MainMenu/CharSelect/Gameplay） |
| m_gameMap | unique_ptr\<Map\> | 当前地图 |
| m_activePlayer | unique_ptr\<Player\> | 当前上场角色（CS 或 Chem） |
| m_enemies | vector\<unique_ptr\<Enemy\>\> | 敌人列表 |
| m_skills | vector\<unique_ptr\<Skill\>> | 角色技能列表 |
| m_pickups | vector\<ItemPickup\> | 道具列表 |
| m_floatTexts | vector\<FloatText\> | 飘字特效列表 |
| m_showSettingsPanel | bool | 设置面板显示标志 |
| m_showCharSelect | bool | 角色选择弹窗标志 |
| m_showLevelUp | bool | 升级选择 UI 标志 |
| m_masterVolume / m_bgmVolume / m_sfxVolume | float | 音量控制 |
| m_blueStackCount | int | 蓝屏叠层数 |
| m_blueStunTimer | float | 僵直剩余时间 |
| m_isDead | bool | 玩家死亡标志 |
| m_killCount | int | 击杀敌人数量 |
| m_difficultyFactor | float | 敌人强度系数 |

### Game.h 函数
| 函数名 | 访问级别 | 说明 |
|--------|----------|------|
| run() | public | 游戏主循环 |
| handleEvents() | private | 处理 SFML 事件 |
| update(dt) | private | 每帧更新逻辑 |
| render() | private | 每帧渲染 |
| setupGufengStyle() | private | 设置古风 UI 配色 |
| renderMainUI() | private | 渲染主菜单 |
| renderSettingsPanel() | private | 渲染设置面板 |
| renderCharSelectPopup() | private | 渲染角色选择弹窗 |
| enterGameplay() | private | 进入游戏场景 |
| updateGameplay(dt) | private | 更新游戏玩法逻辑 |
| renderHUD() | private | 渲染 HUD（血条、经验条） |
| renderDeathScreen() | private | 渲染死亡界面 |
| initEnemies() | private | 初始化敌人 |
| updateEnemies(dt) | private | 更新敌人状态 |
| initSkills() | private | 初始化技能 |
| updateSkills(dt) | private | 更新技能状态 |
| renderSkills() | private | 渲染技能特效 |
| spawnPickup(type, pos, expValue) | private | 生成道具 |
| playBGM(type) | private | 播放背景音乐 |
| getEnemies() | public | 获取敌人列表引用 |

### 关联文件
| 文件 | 用途 |
|------|------|
| `src/game/Enemy.h/cpp` | 敌人基类及派生类，Game 通过 m_enemies 管理 |
| `src/game/Skill.h/cpp` | 技能基类及派生类，Game 通过 m_skills 管理 |
| `src/game/ItemPickup.h/cpp` | 道具类，Game 通过 m_pickups 管理 |
| `src/player/Player.h/cpp` | 玩家基类 |
| `src/player/CSPlayer.h/cpp` | 计算机学生派生类 |
| `src/player/ChemPlayer.h/cpp` | 化学学生派生类 |
| `src/map/Map.h/cpp` | 地图类，提供碰撞检测和渲染 |
| `src/map/MapManager.h/cpp` | 地图管理器 |

---

## 3. 玩家系统

### 3.1 src/player/Player.h / src/player/Player.cpp

#### 文件说明
玩家抽象基类，定义所有玩家的通用属性和接口。

#### 变量
| 变量名 | 类型 | 访问级别 | 说明 |
|--------|------|----------|------|
| m_hp | int | protected | 当前生命值 |
| m_maxHp | int | protected | 最大生命值 |
| m_attack | int | protected | 攻击力 |
| m_defense | int | protected | 防御力 |
| m_speed | float | protected | 移动速度（像素/秒） |
| m_critRate | float | protected | 暴击率（0~1） |
| m_color | sf::Color | protected | 代表色 |
| m_level | int | protected | 当前等级 |
| m_exp | int | protected | 当前经验值 |
| m_expToNext | int | protected | 升级所需经验值 |
| m_type | PlayerType | private | 玩家类型（CS 或 Chem） |
| m_name | std::string | private | 玩家名称 |
| m_position | sf::Vector2f | private | 当前位置（世界坐标） |

#### 函数
| 函数名 | 参数 | 返回值 | 说明 |
|--------|------|--------|------|
| init() | void | void | 纯虚函数，派生类初始化属性 |
| update(dt) | float | void | 纯虚函数，每帧更新 |
| attack() | void | void | 纯虚函数，攻击动作 |
| takeDamage(damage) | int | void | 受到伤害（扣除防御） |
| heal(amount) | int | void | 恢复生命值 |
| addExp(amount) | int | void | 添加经验值（达上限自动升级） |
| isAlive() | void | bool | 检查是否存活 |
| getHp() / getMaxHp() / getAttack() / getDefense() | - | int/float | Getter 方法 |
| getPosition() / setPosition(pos) | sf::Vector2f | const&/void | 位置访问 |

#### 继承关系
```
Player（抽象基类）
├── CSPlayer（计算机学院学生）
└── ChemPlayer（化学学院学生）
```

#### 关联文件
- `src/player/CSPlayer.h/cpp` — 派生类
- `src/player/ChemPlayer.h/cpp` — 派生类
- `src/game/Game.cpp` — 创建 Player 实例

---

### 3.2 src/player/CSPlayer.h / src/player/CSPlayer.cpp

#### 文件说明
计算机学院学生派生类，速度快、攻击高、暴击率高的快攻型职业。

#### 变量（继承自 Player）
| 属性 | 初始值 | 说明 |
|------|--------|------|
| m_maxHp | 80 | 血量较低 |
| m_attack | 15 | 攻击力较高 |
| m_defense | 4 | 防御力较低 |
| m_speed | 200.0f | 移动速度最快 |
| m_critRate | 0.20f | 暴击率 20% |
| m_color | 科技蓝 (60,140,255) | 蓝色主题 |

#### 函数
| 函数名 | 说明 |
|--------|------|
| init() | 初始化 CS 学生属性 |
| update(dt) | 由 Game.cpp 统一处理移动逻辑 |
| attack() | 普通攻击（高暴击单体伤害） |

#### 关联文件
- `src/player/Player.h/cpp` — 基类

---

### 3.3 src/player/ChemPlayer.h / src/player/ChemPlayer.cpp

#### 文件说明
化学学院学生派生类，血量厚、防御高的坦克型职业。

#### 变量（继承自 Player）
| 属性 | 初始值 | 说明 |
|------|--------|------|
| m_maxHp | 140 | 血量最高 |
| m_attack | 10 | 攻击力较低 |
| m_defense | 10 | 防御力最高 |
| m_speed | 140.0f | 移动速度较慢 |
| m_critRate | 0.05f | 暴击率 5% |
| m_color | 化学绿 (50,200,100) | 绿色主题 |

#### 函数
| 函数名 | 说明 |
|--------|------|
| init() | 初始化化学学生属性 |
| update(dt) | 由 Game.cpp 统一处理移动逻辑 |
| attack() | 普通攻击（高防御减免） |

#### 关联文件
- `src/player/Player.h/cpp` — 基类

---

## 4. 敌人系统

### 4.1 src/game/Enemy.h / src/game/Enemy.cpp

#### 文件说明
敌人基类及三个派生类定义文件。

#### Enemy 基类变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| m_type | Type | 敌人类型 |
| m_pos | sf::Vector2f | 位置 |
| m_size | sf::Vector2f | 尺寸 |
| m_hp / m_maxHp | int | 生命值 |
| m_touchDamage | bool | 是否触碰伤害 |
| m_touchDps | int | 触碰 DPS |
| m_expValue | int | 死亡掉落经验值 |
| m_segfaultTimer | float | 冻结动画计时 |
| m_expDropped | bool | 是否已掉落经验 |
| m_mapW / m_mapH | float | 地图边界 |

#### Enemy 基类函数
| 函数名 | 说明 |
|--------|------|
| getBounds() | 获取碰撞包围盒 |
| isDead() / isAlive() | 死亡检查 |
| takeDamage(dmg) | 受到伤害 |
| applySegfault(duration, pendingDmg) | 应用冻结效果 |
| checkTouchDamage(playerHitbox, dt, playerExp) | 触碰伤害检查（派生类实现） |
| getPendingDamage(dt) | 获取延迟伤害（派生类实现） |
| isDashing() | 是否处于冲刺状态（派生类实现） |
| ignoreMapCollision() | 是否忽略地图碰撞（派生类实现） |

#### 继承关系
```
Enemy（抽象基类）
├── DashEnemy — 蓄力冲撞型（状态机：Idle→Alert→Charge→Dash→Recover）
├── BlueScreenEnemy — 叠层眩晕型（触碰叠层，叠满僵直）
└── ThiefEnemy — 经验偷窃型（偷取经验后逃跑）
```

---

### 4.2 DashEnemy（蓄力冲撞型）

#### 变量
| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| m_state | AIState::Idle | AI 状态机状态 |
| m_detectRange | 480.f | 警戒范围（像素） |
| m_visionAngle | 120.f | 视野角度（度） |
| m_chargeTime | 1.2f | 蓄力时间 |
| m_dashSpeed | 620.f | 冲刺速度 |
| m_dashMaxDist | 700.f | 最大冲刺距离 |
| m_dashDir | sf::Vector2f | 冲刺方向 |
| m_dashDamage | 10 | 冲刺伤害 |
| m_slowAmount | 0.45f | 减速比例 |

#### AI 状态机
```
Idle（待机）→ Alert（警戒，发现玩家但不在视野）
           → Charge（蓄力，玩家在视野内）
           → Dash（冲刺）
           → Recover（恢复）
           → Idle（回到待机）
```

#### 关联文件
- `src/game/Enemy.h/cpp` — 基类 Enemy
- `src/game/Game.cpp` — 创建 DashEnemy 实例

---

### 4.3 BlueScreenEnemy（叠层眩晕型）

#### 变量
| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| m_stackCount | 0 | 当前叠层数（上限10层） |
| m_touchDps | 8 | 触碰 DPS |
| m_touchCdTimer | 0.f | 触碰冷却计时 |
| m_moveSpeed | 80.f | 移动速度 |
| m_detectRange | 350.f | 警戒范围 |
| m_velocity | sf::Vector2f | 当前速度 |

#### 机制
- 触碰敌人时叠层（1层/秒）
- 叠满10层触发僵直，玩家可趁机击杀

#### 关联文件
- `src/game/Enemy.h/cpp` — 基类 Enemy
- `src/game/Game.cpp` — 管理叠层计数

---

### 4.4 ThiefEnemy（经验偷窃型）

#### 变量
| 变量名 | 说明 |
|--------|------|
| m_state | ThiefState（Idle/Alert/Chase/Flee） |
| m_stolenExpTotal | 累计偷取经验 |
| m_lastStolenAmount | 最近一次偷取经验 |
| m_fleeSpeed | 逃跑速度 |
| m_baseSpeed | 基础速度 |

#### 机制
1. 发现玩家后追击
2. 接触玩家时偷取经验
3. 偷取后朝反方向逃跑

#### 关联文件
- `src/game/Enemy.h/cpp` — 基类 Enemy
- `src/player/Player.cpp` — addExp() 处理经验扣除

---

## 5. 技能系统

### 5.1 src/game/Skill.h / src/game/Skill.cpp

#### 文件说明
技能基类及六个职业专属技能派生类。

#### Skill 基类变量
| 变量名 | 说明 |
|--------|------|
| m_id | 技能唯一标识 |
| m_name | 技能显示名称 |
| m_internalName | 内部名称（用于资源路径） |
| m_desc | 技能描述 |
| m_level | 当前等级 |
| m_cooldownTimer | 冷却计时 |
| m_cooldownMax | 冷却时间 |

#### Skill 基类函数
| 函数名 | 说明 |
|--------|------|
| update(dt, playerPos, gameCtx) | 纯虚，每帧更新 |
| render(window, playerPos) | 纯虚，渲染技能特效 |
| getUpgrades() | 纯虚，获取升级选项 |
| applyUpgrade(statName, delta) | 纯虚，应用升级效果 |
| getId() / getLevel() / getName() | Getter 方法 |

#### SkillUpgrade 结构体
| 成员 | 类型 | 说明 |
|------|------|------|
| label | std::string | 显示文本 |
| statName | std::string | 属性名称 |
| delta | float | 属性增量 |
| iconId | int | 图标 ID |

#### 继承关系
```
Skill（抽象基类）
├── PointerStormSkill — CS 技能，指针风暴弹幕
├── MagicCircleSkill — CS 技能，校徽法阵持续伤害
├── KoiShieldSkill — CS 技能，锦鲤环绕护盾
├── ChemCircleSkill — Chem 技能，分子法阵
├── ExothermicStormSkill — Chem 技能，放热风暴
└── ChemOrbitSkill — Chem 技能，炼金环绕球
```

---

### 5.2 PointerStormSkill（指针风暴）

#### 变量
| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| m_range | 250.f | 技能范围（像素） |
| m_damage | 30 | 每次命中伤害 |
| m_maxTargets | 3 | 最大目标数 |
| m_hitEffects | vector\<HitEffect\> | 命中特效列表 |

#### 机制
- 冷却型技能（2秒冷却）
- 对范围内最近的3个敌人造成伤害
- 保留 segfault 视觉效果

---

### 5.3 MagicCircleSkill（校徽法阵）

#### 变量
| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| m_radius | 140.f | 法阵半径 |
| m_damage | 8 | 每 tick 伤害 |
| m_tickInterval | 0.8f | 伤害间隔 |
| m_ringAngle | float | 法环旋转角度 |
| m_ringSpeed | 45.f | 法环旋转速度 |

#### 机制
- 被动型技能（无冷却）
- 持续环绕玩家，每 0.8 秒对范围内敌人造成伤害

---

### 5.4 KoiShieldSkill（锦鲤护盾）

#### 变量
| 变量名 | 初始值 | 说明 |
|--------|--------|------|
| m_koiCount | 2 | 锦鲤数量 |
| m_damage | 15 | 碰撞伤害 |
| m_orbitSpeed | π rad/s | 公转速度 |
| m_radius | float | 公转半径 |
| m_koi | vector\<Koi\> | 锦鲤列表 |

#### 机制
- 被动型技能
- 多条锦鲤环绕玩家旋转，接触敌人造成伤害
- 锦鲤死亡后自动复活

---

### 5.5 ChemCircleSkill / ExothermicStormSkill / ChemOrbitSkill

#### ChemCircleSkill（分子法阵）
- 持续型法阵技能，机制类似 MagicCircleSkill

#### ExothermicStormSkill（放热风暴）
- 机制类似 PointerStormSkill，区别在于使用化学相关纹理

#### ChemOrbitSkill（炼金环绕球）
- 机制类似 KoiShieldSkill，使用炼金球体

---

## 6. 道具系统

### 6.1 src/game/ItemPickup.h / src/game/ItemPickup.cpp

#### 文件说明
游戏中的可拾取道具（回血、加速、经验）。

#### PickupType 枚举
| 类型 | 说明 |
|------|------|
| Heal | 回血道具，恢复玩家生命 |
| Speed | 加速道具，增加移动速度 |
| Exp | 经验道具，提供额外经验值 |

#### 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| m_type | PickupType | 道具类型 |
| m_position | sf::Vector2f | 位置 |
| m_size | float | 碰撞尺寸 |
| m_expValue | int | 经验值（仅经验道具） |
| m_age | float | 已存在时间 |
| m_lifetime | float | 存活时间（15秒） |
| m_bobOffset | float | 上下浮动偏移 |
| m_sprite | sf::Sprite | 精灵渲染 |

#### 函数
| 函数名 | 说明 |
|--------|------|
| update(dt) | 更新浮动动画和超时消失 |
| render(window) | 渲染道具精灵 |
| getBounds() | 获取碰撞包围盒 |

#### 关联文件
- `src/game/Game.cpp` — 管理道具生成和拾取检测

---

## 7. 地图系统

### 7.1 src/map/Map.h / src/map/Map.cpp

#### 文件说明
地图类，负责加载 Tiled 格式地图和碰撞检测。

#### 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| m_name | std::string | 地图名称 |
| m_rows / m_cols | int | 地图行数和列数 |
| m_tileWidth / m_tileHeight | int | 单个 Tile 尺寸（像素） |
| m_layerData | vector\<vector\<vector\<int\>\>\> | 图层数据（三维：图层→行→列） |
| m_layerNames | vector\<std::string\> | 图层名称列表 |
| m_tilesets | vector\<TilesetInfo\> | Tileset 信息列表 |
| m_collisions | vector\<CollisionRect\> | 碰撞矩形列表 |
| m_polygons | vector\<CollisionPolygon\> | 碰撞多边形列表 |
| m_spawnPoint | sf::Vector2f | 玩家出生点 |
| m_exitPoint | sf::Vector2f | 地图出口点 |

#### 函数
| 函数名 | 参数 | 返回值 | 说明 |
|--------|------|--------|------|
| loadFromTiled(tmxPath) | const std::string& | bool | 从 Tiled 文件加载地图 |
| getTile(row, col) | int, int | Tile& | 获取指定位置 Tile |
| isWalkable(row, col) | int, int | bool | 检查是否可行走 |
| isColliding(x, y) | float, float | bool | 检查点是否碰撞 |
| isRectColliding(rx, ry, rw, rh) | float, float, float, float | bool | 检查矩形是否碰撞 |
| getSpawnPoint() / getExitPoint() | - | sf::Vector2f | 获取出生点/出口点 |
| render(window, tileSize) | sf::RenderWindow&, int | void | 渲染地图 |
| getRows() / getCols() | - | int | 获取地图尺寸 |

#### TilesetInfo 结构体
| 成员 | 类型 | 说明 |
|------|------|------|
| firstGid | int | 起始 GID |
| texture | sf::Texture | 纹理 |
| loaded | bool | 是否加载成功 |
| columns | int | 列数 |
| spacing | int | 间距 |
| margin | int | 边距 |
| tileCount | int | Tile 总数 |

#### CollisionRect 结构体
| 成员 | 类型 | 说明 |
|------|------|------|
| x, y | float | 左上角坐标 |
| width, height | float | 宽高 |

#### 关联文件
- `src/game/Game.cpp` — 创建 Map 实例进行游戏场景渲染
- `src/map/MapManager.h/cpp` — 管理多张地图切换

---

### 7.2 src/map/MapManager.h / src/map/MapManager.cpp

#### 文件说明
地图管理器，负责从 assets/maps/ 目录加载所有 .tmx 地图文件。

#### 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| m_maps | vector\<unique_ptr\<Map\>\> | 地图列表 |
| m_currentIndex | int | 当前地图索引 |

#### 函数
| 函数名 | 说明 |
|--------|------|
| loadMaps() | 从 maps/ 目录加载所有 .tmx 文件 |
| getCurrentMap() | 获取当前地图引用 |
| switchMap(mapName) | 按名称切换地图 |
| getMapCount() | 获取地图总数 |

#### 关联文件
- `src/map/Map.h/cpp` — Map 类

---

### 7.3 src/map/Tile.h / src/map/Tile.cpp

#### 文件说明
单个 Tile（地块）类，表示地图中的一个格子。

#### TileType 枚举
| 类型 | 说明 |
|------|------|
| Floor | 地板，可行走 |
| Wall | 墙壁，不可通行 |
| Spawn | 出生点 |
| Exit | 出口 |
| Collision | 碰撞层 |

#### 变量
| 变量名 | 类型 | 说明 |
|--------|------|------|
| m_type | TileType | Tile 类型 |
| m_row / m_col | int | 所在行列 |
| m_texTileX / m_texTileY | int | 纹理坐标 |

#### 函数
| 函数名 | 说明 |
|--------|------|
| isWalkable() | 是否可行走（Floor 或 Spawn） |
| getType() | 获取 Tile 类型 |
| setType(type) | 设置 Tile 类型 |
| setTextureRect(tileX, tileY) | 设置纹理坐标 |

#### 关联文件
- `src/map/Map.h/cpp` — Map 使用 Tile 存储地图数据

---

## 文件关联总览

```
main.cpp
    └── Game

Game
    ├── Map (地图渲染、碰撞检测)
    │   └── Tile
    ├── MapManager (地图管理)
    │   └── Map
    ├── Player (玩家基类)
    │   ├── CSPlayer (计算机学生)
    │   └── ChemPlayer (化学学生)
    ├── Enemy (敌人基类)
    │   ├── DashEnemy (蓄力冲撞型)
    │   ├── BlueScreenEnemy (叠层眩晕型)
    │   └── ThiefEnemy (经验偷窃型)
    ├── Skill (技能基类)
    │   ├── PointerStormSkill
    │   ├── MagicCircleSkill
    │   ├── KoiShieldSkill
    │   ├── ChemCircleSkill
    │   ├── ExothermicStormSkill
    │   └── ChemOrbitSkill
    └── ItemPickup (道具)
```

---

## 数据流概述

### 游戏初始化流程
```
main() → Game() 构造函数
       → 加载主菜单背景纹理
       → 初始化 ImGui
       → 加载字体和古风样式
       → 加载视频帧动画
       → 加载角色头像
       → 播放菜单 BGM
```

### 进入游戏流程
```
renderCharSelectPopup() 选择角色
    → enterGameplay()
       → MapManager::loadMaps() 加载地图
       → 创建 Player 实例（CSPlayer 或 ChemPlayer）
       → initEnemies() 初始化敌人
       → initSkills() 初始化技能
       → 切换到 Gameplay 状态
       → 播放游戏 BGM
```

### 每帧更新流程
```
run() 主循环
    → handleEvents() 处理输入
    → update(dt)
    │   ├── updateGameplay(dt)
    │   │   ├── 更新玩家位置（键盘输入 + 碰撞检测）
    │   │   ├── updateEnemies(dt) 更新敌人 AI
    │   │   ├── updateSkills(dt) 更新技能效果
    │   │   ├── updatePickups(dt) 更新道具
    │   │   ├── 检查道具拾取
    │   │   ├── checkDeath() 检查死亡
    │   │   └── 更新飘字特效
    │   └── 渲染所有内容
    └── render()
        ├── 渲染地图
        ├── 渲染道具
        ├── 渲染敌人
        ├── 渲染玩家
        ├── 渲染技能特效
        └── 渲染 HUD
```

---

*文档生成时间：2026-05-21*
